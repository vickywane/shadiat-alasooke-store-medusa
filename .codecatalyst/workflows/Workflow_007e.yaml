Name: Workflow_007e
SchemaVersion: "1.0"

# Optional - Set automatic triggers.
Triggers:
  - Type: Push
    Branches:
      - main

# Required - Define action configurations.
Actions:
    Build:
        Identifier: aws/build@v1
        Configuration:
            Steps:
                - Run: echo "Hey there!!"